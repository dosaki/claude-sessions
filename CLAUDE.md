# CLAUDE.md - claude-sessions

## Project Overview

A dashboard tool that displays all running Claude Code sessions. Supports a native GUI window (default, powered by Fyne) and a terminal-based dashboard (`--cli`). Written in Go, cross-platform (macOS, Linux, Windows).

## Build & Run

```bash
make build                                               # build binary
make test                                                # run all tests
make app                                                 # macOS .app bundle (VERSION=dev default)
make app VERSION=1.2.3                                   # macOS .app bundle with specific version
go run ./cmd/claude-sessions --once                      # run without building
./claude-sessions-bin --once                             # quick smoke test
```

Requires Go 1.24+ and CGO (for Fyne's OpenGL bindings). External dependency: `fyne.io/fyne/v2`.

## Code Organization

```
cmd/claude-sessions/
  main.go                            # Entry point: flags, signals, GUI/CLI dispatch
  console_windows.go                 # Windows: AttachConsole for --cli/--once output
  console_other.go                   # No-op on non-Windows
internal/
  ansi/ansi.go                       # ANSI constants + string helpers (VisibleLen, Pad, Truncate, Clamp)
  config/config.go                   # Config type (Once, GUI, Compact, Interval, CPUThreshold, Sort, Filter)
  discovery/                         # Process discovery + session classification
    classify.go                      #   Session, RawProc types; ClassifySessions, DetermineStatus
    filter.go                        #   IsClaudeLine, DetectIDEFromCmd, idePatterns
    filter_projects.go               #   FilterSessions (by project path), UniqueProjects
    jsonl.go                         #   SessionState type; HomeDir; JSONL state reading
    sort.go                          #   SortSessions, IsValidSortColumn, parseElapsed
    platform_unix.go                 #   GetClaudeProcesses, GetWorkDir, GetSource (ps/lsof/proc)
    platform_windows.go              #   GetClaudeProcesses, GetWorkDir, GetSource (PowerShell)
  focus/                             # Session focusing (bring terminal/IDE to foreground)
    cgwindows_darwin.go              #   CoreGraphics window listing (macOS, via cgo)
    cgwindows_other.go               #   No-op stub for non-darwin/non-windows
    focus_unix.go                    #   FocusSession: tmux, macOS AppleScript, IDE CLIs, process tree walk
    focus_windows.go                 #   FocusSession: IDE CLIs only
    match.go                         #   bestMatch: window-to-session matching by WorkDir/title
    window.go                        #   Window type (AppName, Title, WorkDir, TTY, Index, PaneID)
  gui/                               # Fyne-based native GUI dashboard
    gui.go                           #   Run(): Fyne app, table, sort, filter dialog, notifications
    gui_format.go                    #   Formatting helpers (statusColor, statusLabel, formatPercent, etc.)
    theme.go                         #   Custom dark theme (Catppuccin-inspired)
  render/render.go                   # CLI dashboard rendering: row/summary formatting, Draw()
  terminal/                          # Terminal abstraction
    resize.go                        #   ResizeRequested (shared atomic)
    defaults.go                      #   Default terminal dimensions
    input.go                         #   Key type definition
    input_unix.go                    #   EnableRawMode, DisableRawMode, ReadKeys (Unix)
    input_windows.go                 #   EnableRawMode, DisableRawMode, ReadKeys (Windows)
    terminal_unix.go                 #   GetSize (ioctl), Init (SIGWINCH + SIGTERM)
    terminal_windows.go              #   GetSize (Console API), Init (VT enable + polling)
  util/format.go                     # Shared formatting helpers (ShortenPath, FormatPercent, TruncateProject)
assets/                                # Generated at build time (gitignored)
  Info.plist                         #   Generated by `make app` with VERSION
  icon.icns                          #   Generated by `make icon` from docs/logo.svg
docs/
  logo.svg                           # Source icon (committed; used to generate icon.icns)
install.sh                           # macOS/Linux installer (curl one-liner)
install.ps1                          # Windows installer (PowerShell one-liner)
.github/workflows/                   # CI/CD pipelines
  ci.yml                             #   Build + test on push/PR
  release.yml                        #   Build + publish GitHub releases
Makefile                             # build, test, app, build-windows, icon, clean targets
```

**Dependency graph** (no circular deps):
- `ansi` → nothing
- `config` → nothing
- `discovery` → nothing
- `util` → `discovery`
- `focus` → `discovery`
- `gui` → `config`, `discovery`, `focus`, `util`
- `terminal` → `ansi` (for CursorShow in SIGTERM handler)
- `render` → `ansi`, `config`, `discovery`, `terminal`, `util`
- `cmd/main` → `ansi`, `config`, `discovery`, `focus`, `gui`, `render`, `terminal`

## Coding Philosophy

- Follow **Go idiomatic patterns** and best practices - effective error handling, clear naming, minimal abstractions
- When not contradicting with the Go practices, follow **DRY** (Don't Repeat Yourself) principles - extract shared logic, components, and styles
- Be **pragmatic** - favour working, readable code over clever abstractions
- Keep files **organised and focused** - avoid dumping many components, types, or interfaces into a single file; split by concern

## Constraints

- External dependency: Fyne (`fyne.io/fyne/v2`) for GUI mode. CGO required
- Cross-platform: macOS, Linux, Windows (platform-specific code uses build tags)
- ANSI escape sequences must be handled when measuring/truncating strings - use `ansi.VisibleLen()` and `ansi.Truncate()`
- CPU is checked first for status; JSONL state is the secondary signal (see `discovery.DetermineStatus`)
- Windows limitations: CPU% is unavailable (status relies on JSONL); CWD detection is best-effort; session focus is IDE-only

## Common Tasks

- **Add IDE detection**: add entry to `idePatterns` in `internal/discovery/filter.go`, or update parent-process switch in `platform_unix.go`/`platform_windows.go`
- **Add column**: update `Session` struct in `classify.go` → populate in `ClassifySessions()` → adjust CLI rendering in `render.go` → add GUI column in `gui.go`
- **Add sort column**: add to `ValidSortColumns` and `SortSessions` switch in `internal/discovery/sort.go` → add to `columnSortKeys` in `gui.go`
- **Add focus support for new terminal**: add to `terminalPatterns` in `internal/focus/focus_unix.go` → add case in `FocusSession()` if special logic needed
- **Change status logic**: modify `DetermineStatus()` in `internal/discovery/classify.go`
- **Modify GUI theme**: edit `internal/gui/theme.go`
- **Add ANSI helpers**: add to `internal/ansi/ansi.go`
- **Add terminal source**: update parent-process switch in `platform_unix.go` and/or `platform_windows.go`
